package com.soraxus.prisons.util.display.animation.objects;

public class EaseAnimation<T> extends Animation<T> {
    public EaseAnimation(T... elements) {
        int len = elements.length;
        double d = len / Math.PI;

    }
}
