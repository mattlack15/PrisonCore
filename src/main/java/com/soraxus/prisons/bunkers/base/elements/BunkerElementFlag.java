package com.soraxus.prisons.bunkers.base.elements;

public enum BunkerElementFlag { //Only ever remove flag types when in development, or right before a reset. (Serialization compatibility reasons)
    PROTECTED,
    SHIELDED;
}
